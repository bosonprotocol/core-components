import { styled } from "styled-components";
import React from "react";

const Wrapper = styled.div``;

export type ProductsRobloxProps = {
  a?: null;
};
export const ProductsRoblox = ({ a }: ProductsRobloxProps) => {
  return <Wrapper>products</Wrapper>;
};
