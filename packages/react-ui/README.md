# `@bosonprotocol/react-ui

`

List os react visual components and storybook stories for them that allow to easily test them.

## INSTALLATION

run on the root of the project:

```bash
npm ci
```

## Usage

run inside of react-ui folder:

```bash
npm run storybook
```
